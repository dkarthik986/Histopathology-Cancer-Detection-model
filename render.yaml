services:
  - type: web
    name: histopathology-image-cancer-prediction
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 180 --workers 1 --threads 1 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
